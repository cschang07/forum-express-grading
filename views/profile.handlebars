<div class="card mb-3" style="max-width: auto;">
  <div class="row g-0">
    <div class="col-md-4">
      <img src="{{user.image}}" class="img rounded-circle mx-3 my-3" style="width: 200px; height: 200px" alt="...">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">{{user.name}}</h5>
        <p class="card-text">{{user.email}}</p>
        <a href='/users/{{user.id}}/edit'>
          <button type="button" class="btn btn-primary">Edit Profile</button>
        </a>
      </div>
    </div>
  </div>
</div>

<a href="javascript:history.back()">回上一頁</a>

<br>
<br>
<div class="card">
  <div class="card-header">
    已評論餐廳
  </div>
  <div class="card-body">
    {{#each comments}}
    <a href="/restaurants/{{this.Restaurant.id}}" style="display: inline-block;">
      <img class="img" src="{{this.Restaurant.image}}" style="width: 100px; height:100px">
    </a>
    {{/each}}
  </div>
</div>
</div>